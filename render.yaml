services:
  - type: web
    name: media-downloader
    env: python
    buildCommand: |
      sudo apt-get update && sudo apt-get install -y ffmpeg
      pip install -r requirements.txt
    # 💡 NEW START COMMAND with increased timeout and multiple workers
    startCommand: gunicorn --timeout 120 -w 2 app:app 
    plan: starter 
    rootDir: .
